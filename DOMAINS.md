# 梦新领域开发指南 — 加新领域 3 步（框架通用化）

> 梦新是通用框架。漏洞/法律只是两个领域实例。加任何新领域不用改主流程。

## 加新领域的 3 步

### 1. 写模板（定义字段 + 红线）
templates/08_<领域>.txt
- 说明: 这个领域输出什么 JSON（字段名 + 类型）
- 红线: 原文没有的字段必须 null/unknown（禁止编造）
- 免责/边界: 非专业领域要加"仅供参考, 不构成XX意见"

### 2. 写提取函数（从原文抽字段）
run_model.py 里加 extract_<领域>(raw_text) -> dict
- 正则/规则从 input.txt 原文提取
- 查不到 → null（红线）
- evidence 必须逐字来自原文

### 3. 注册一行（领域注册表）
EXTRACTORS["08_<领域>"] = extract_<领域>
- 一个领域 = 一行注册，不改 if 链

## 判卷（自动）
verify.py 从 expected.json 读字段递归判卷（已通用, 不用改）

## 记忆体（可选）
memory-body/knowledge/<领域>.md — 领域知识/实例/技能锚

## 联网（可选）
run_model._register_source("<源名>", fn) — 领域专用检索源

## 已注册领域
01 漏洞分析 | 03 内部扫描 | 05 好奇心 | 06 代码安全 | 07 法律问答
